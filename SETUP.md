# Ledger Starter

カードの利用通知メールを、Notionの家計簿に自動で1行足す仕組みです。対応カードは三井住友・楽天カード・ビューカード（JALカードSuica・ルミネカード・JRE CARD等）の3社です。この3社のカードの利用は、手で入力しなくてよくなります。

所要時間は目安30分です。プログラミングの経験は要りません。コピーと貼り付けだけで動きます。Discordのボタンで分類を直したい場合は、手順7（任意）で15〜25分ほど追加になります。

## 用意するもの

- Gmailアカウント（カードの利用通知メールが届いているもの）。
- Notionアカウント（無料プランで十分です）。

## 手順

### 1. Notionにデータベースを作る

`notion-template.md` の手順どおりに、家計簿用のデータベースを1個作ります。「名前・金額・日付・カード・分類」という5つの欄を持つ表です。ここで作った表に、手順3で貼るプログラムが自動で行を足します。

### 2. Notion統合トークンを取得する

「統合トークン」とは、外部のプログラムがNotionに書き込むための合言葉です。以下の手順で発行します。

1. ブラウザで https://www.notion.so/my-integrations を開きます。
2. 「新しいインテグレーション」を作成します。名前は「Ledger Starter」など好きな名前で構いません。
3. 発行された「シークレット」（`ntn_` または `secret_` から始まる文字列）をコピーします。
4. 手順1で作ったデータベースのページを開きます。右上の「…」→「接続」→「接続先を追加」の一覧から、3で作ったインテグレーションを選びます。「（名前）をこのページに追加しますか」という確認が出るので「ページに追加」を押します。

4を忘れると、トークンが正しくてもNotionが「そのデータベースは見えない」として書き込みを拒否します。あとで出るエラー文は「Could not find database with ID」です。データベースを別のページの中に作った場合は、親ページで接続しても構いません（中のデータベースにも効きます）。

### 3. Google Apps Scriptにコードを貼る

「Google Apps Script」（略してGAS）は、Googleが無料で提供するプログラム実行環境です。Gmailを読んでNotionに書き込む処理はここで動かします。

1. ブラウザで https://script.google.com を開き、「新しいプロジェクト」を作ります。
2. プロジェクト名を「Ledger Starter」などに変えます。
3. 左のファイル一覧にある `コード.gs` の中身を全部消し、この配布物の `Code.gs` の中身を貼り付けます。
4. 左の「＋」→「スクリプト」で新しいファイルを作り、名前を `Config` にします（拡張子の `.gs` は自動で付きます）。
5. `Config.gs` の中身を貼り付けます。`NOTION_TOKEN` は手順2で控えた値に、`NOTION_DATABASE_ID` は手順1のデータベースIDに書き換えます。

`Config.gs` に貼った統合トークンはそのままコードに残ります。このApps Scriptプロジェクトを他人と共有したり、コードを公開したりしないでください。

### 4. 1分おきに動かす設定をする

「トリガー」とは、GASの処理を自動で起動する予約設定です。時計のアイコン（左メニューの「トリガー」）を開き、以下のとおり追加します。

- 実行する関数: `checkCardEmails`
- イベントのソース: 時間主導型
- 時間ベースのトリガーのタイプ: 分ベースのタイマー
- 時間の間隔: 1分おき
- エラー通知設定: 今すぐ通知を受け取る（既定の「1日に1回」のままだと、設定ミスに気付くのが最大24時間遅れます）

「実行するデプロイ」という欄もありますが、「Head」のままで構いません。

保存すると、初回だけGoogleの確認画面が出ます。順番は次のとおりです。

1. 「このアプリはGoogleで確認されていません」と出ます。「詳細」→「Ledger Starter（安全ではないページ）に移動」を押します。
2. 「Ledger Starterがアクセスできる内容を選択」と出ます。「すべて選択」にチェックが入っていることを確かめて（既定でチェック済みの場合もあります）「続行」を押します。チェックが外れていると権限が付かず、次の実行が失敗します。

2の画面には「Gmailのすべてのメールの閲覧、作成、送信、完全な削除」と書かれます。このプログラムがGmailに対してするのは、カード会社のメールを読むことと、ラベルを付けることだけです。`Code.gs` の中でも確かめられます。

許可する相手は、あなたが自分のアカウントで作ったこのプロジェクトです。プログラムが外に送るのは利用の日付・店名・金額・カード名・分類で、送り先はあなたのNotionのデータベースだけです。

### 5. 動作確認をする

1〜2分待ってから、Notionのデータベースを開きます。直近のカード利用が増えていれば成功です。

初回は、過去2日以内に届いた通知メールをまとめて記帳します。ビューカードだけは過去10日以内です。それより前のメールは読みません。その期間をすでに手で入力していた場合は、重なった行を消してください。

GASの画面で「実行」ボタンを押すと、1分を待たずに確かめられます。上の「checkCardEmails」が選ばれているのを確かめてから押してください。

増えていない場合はGmailを確認します。処理されたメールには「Ledger Starter」というラベルが自動で付きます。ラベルは会話（スレッド）単位で付くので、同じ件名の通知が1つにまとまっていれば、まとめて1か所に付きます。

- **GASの「実行数」タブに赤い「失敗」が出ている**: `Config.gs` の値か、Notion側の「接続」設定（手順2の4）を見直してください。1件でもエラーがあると実行全体を失敗にする作りなので、最初に見る場所です。
- **「Ledger Starter/記帳済み」が付いている**: その利用は家計簿に載っています。新しく書いた場合と、同じ行が既にあったので書かなかった場合の両方でこの印が付きます。データベースの側を見直してください。
- **「Ledger Starter/要確認」が付いている**: 日付・店名・金額のどれかを読み取れなかった印です。金額は「1,234円」の形（三井住友は「1,234.00JPY」の形も）だけを読みます。外貨で書かれた通知や返品の通知がこれに当たります（下の「できないこと」）。
- **どちらも付いていない**: トリガーがまだ動いていないか、対象のメールが届いていません。三井住友と楽天は2日以内、ビューカードは10日以内のメールが対象です。まれに、記帳はできたのにラベル付けだけが失敗していることがあります。この失敗では通知メールが来ないので、家計簿の側も見てください。

失敗した実行はGoogleが自動でメールを送ってくれるので、待っているだけでも気付けます。

書き込みが1件でも失敗すると、その回はそこで止め、次の1分でその1件から読み直します。即時の通知や速報版より先に、明細や確報版を記帳しないためです。この止め方はカード会社をまたぎます。1通が失敗し続けている間は、その後ろにある他のメール（他のカード会社の分も含む）も記帳を待ちます。待っている間に読む期間（2日、ビューカードは10日）を過ぎたメールは、失敗した1通も後ろのメールも記帳されません。失敗の通知が届いたら、その期間のうちに直してください。

### 6. Discordに通知する（任意）

記帳するたびにDiscordへ1行通知したい場合は、以下を行います。設定しなければ今までどおり通知なしで動きます。

1. Discordでメッセージを送りたいチャンネルの設定を開き、「連携サービス」→「ウェブフックを作成」からWebhook URLを発行します。
2. `Config.gs` の `DISCORD_WEBHOOK_URL` に、発行したURLをそのまま貼ります。
3. GASの画面で保存すれば、次の記帳から反映されます。改めてデプロイし直す必要はありません。

通知される内容は「日付・店名・金額・カード名・分類」の1行だけです。通知が届かない・失敗しても記帳自体は止まりません（GASの「実行数」タブのログに残るだけです）。分類を直したいときはNotionの表を直接編集してください。Discordの通知から直接直したい場合は、次の手順7（任意）でできるようになります。

### 7. Discordのボタンで分類を直す（任意・上級者向け）

手順6の通知に、分類を選び直すセレクトメニューを付けます。Discord上で選ぶだけでNotionの表がその場で書き換わります。手順6より設定が増え、Cloudflareという別サービスのアカウントも要ります。コピーと貼り付けで進められますが、コマンドを打つ場面はありません。

分類を直したいだけなら、Notionの表を直接編集する方法（上記）で十分です。この手順は「Discordだけで完結させたい」場合のためのものです。

**既知の不具合（2026-09-21確認）**: 環境によっては、7-4までの設定を正しく終えても、カードの利用があった時にDiscordへ通知メッセージそのものが届かないことがあります（GoogleのサーバーからDiscordへの投稿がDiscord側で拒否される）。この場合もカードの利用自体は問題なくNotionに記帳され、表を直接編集すれば分類はいつでも直せます。原因を調査中です。

#### 7-1. Discordの「アプリケーション」を作る

手順6のWebhookとは別に、Discordの「開発者ポータル」でアプリケーションを1個作ります。

1. ブラウザで https://discord.com/developers/applications を開きます（Discordのログインが必要です）。
2. 「New Application」を押し、名前を付けて作成します。
3. 左メニュー「General Information」に「Public Key」という欄があります。この値を後で使うのでメモします。
4. 左メニュー「Bot」を開き、「Reset Token」（初回は「Add Bot」の場合もあります）でトークンを発行します。表示は一度きりなので、ここでメモします。これが`Config.gs`の`DISCORD_BOT_TOKEN`です。
5. 同じ「Bot」の画面で「MESSAGE CONTENT INTENT」等の設定はそのままで構いません（このBotはメッセージを読みません。投稿するだけです）。

#### 7-2. Botを自分のサーバーに入れる

1. 左メニュー「OAuth2」→「URL Generator」を開きます。
2. 「SCOPES」で `bot` にチェックを入れます。
3. 下に出る「BOT PERMISSIONS」で `Send Messages` にチェックを入れます。
4. 画面下に生成されるURLを開き、通知を送りたいサーバーを選んで「認証」します。デスクトップ版Discordが入っている場合、ブラウザがアプリへの切り替えを求めたまま進まないことがあります。その画面が出たらブラウザ側で拒否（Block）すると、ブラウザの中で続きの画面が開きます。

#### 7-3. 通知先チャンネルのIDを調べる

1. Discordアプリ（ブラウザ版でもデスクトップ版でも可）の設定→「詳細設定」で「開発者モード」をオンにします。
2. 通知を送りたいチャンネルを右クリックし、「IDをコピー」を選びます。これが`Config.gs`の`DISCORD_CHANNEL_ID`です。

#### 7-4. Config.gsに追記してGASをWebアプリとしてデプロイする

1. GASの`Config.gs`を開き、`DISCORD_BOT_TOKEN`と`DISCORD_CHANNEL_ID`に7-1・7-3の値を貼ります。両方揃うと、手順6のWebhookより優先してこちらの通知に切り替わります（`DISCORD_WEBHOOK_URL`は残したままで構いません）。
2. 保存します。
3. 右上「デプロイ」→「新しいデプロイ」を開きます。**ここまでの手順4・6ではWebアプリのデプロイを一度も作っていません。** 「デプロイを管理」を開いても、編集できる既存のデプロイはまだ無い状態です。手順4の「実行するデプロイ」はトリガーの動作先を選ぶ欄で、Webアプリのデプロイとは別物です。
4. 種類の選択で「ウェブアプリ」を選びます（歯車アイコンで選択肢が出ない場合は種類が最初から「ウェブアプリ」になっています）。
5. 「次のユーザーとして実行」は「自分」、「アクセスできるユーザー」は「全員」を選びます。既定値のままだと限られた人しかアクセスできず、Discordからの通知が届きません。
6. 「デプロイ」を押します。手順4と同じ「このアプリはGoogleで確認されていません」の画面が出ます。「詳細」→「（プロジェクト名）（安全ではないページ）に移動」を押します。表示された権限（Gmail・外部サービスへの接続）を確認して「続行」を押します。これはGmail読み取りとは別に、Webアプリ自身の実行権限を許可する手続きです。
7. 表示された「ウェブアプリ」の「URL」をメモします（`https://script.google.com/macros/s/.../exec`の形）。これが次の手順で使う「GASのURL」です。

以後`Code.gs`や`Config.gs`を書き換えた時は、保存するだけでは反映されません。「デプロイ」→「デプロイを管理」→既存のデプロイの鉛筆アイコン（編集）→「バージョン」を「新バージョン」に変えて「デプロイ」を押し直してください。トリガーで動く`checkCardEmails`は保存するだけで最新のコードが動く点と対照的です。

#### 7-5. Cloudflare Workerを作る（コピーと貼り付けだけ）

wranglerやnpmは使いません。ブラウザの画面だけで完結します。

1. ブラウザで https://dash.cloudflare.com を開きます（無料アカウントで構いません。未登録なら先に作成します）。
2. 左メニュー「Workers & Pages」→「Create」（または「アプリケーションを作成」）→「Create Worker」を選びます。
3. 名前を付けて「Deploy」を押します（まずは空のサンプルコードがデプロイされます）。
4. デプロイ後の画面で「Edit code」（コードを編集）を押します。
5. エディタの中身を全部消し、この配布物の`worker.js`の中身をそのまま貼り付けます。
6. 右上の「Deploy」を押します。
7. 「Settings」（設定）タブ→「Variables and Secrets」（変数とシークレット）を開きます。
8. 「Add」（追加）で1つ目を作ります。名前は`DISCORD_PUBLIC_KEY`、値は7-1でメモしたPublic Key。種類は「Secret」を選びます。
9. もう1つ「Add」します。名前は`GAS_URL`、値は7-4でメモしたウェブアプリのURL。こちらも「Secret」で構いません。
10. 「Deploy」（または「Save」）を押して確定します。
11. Workerの概要画面に出ているURL（`https://（名前）.（アカウント名）.workers.dev`の形）をメモします。

#### 7-6. Discord側にWorkerのURLを登録する

1. 7-1で開いた開発者ポータルのアプリケーションに戻り、「General Information」を開きます。
2. 「Interactions Endpoint URL」欄に、7-5でメモしたWorkerのURLを貼ります。
3. 「Save Changes」を押します。Discordがこの場でWorkerに疎通確認を送り、正しく応答が返らないと保存できません。保存できない場合は7-4のGAS_URLと7-5のDISCORD_PUBLIC_KEYの値を貼り間違えていないか、Workerのデプロイが完了しているかを確認してください。

#### 動作確認

次にカードの利用が記帳されると、Discordの通知に分類のセレクトメニューが付きます。別の分類を選ぶと、数秒以内にDiscord上の表示が選び直した分類に変わり、Notionの表の分類も同じ値に変わります。

セレクトを操作してから**3秒以内**にDiscordへ一次応答が返る必要があります（Discordの仕様）。一次応答はWorkerがその場で返すのでほぼ一瞬ですが、その後のNotionへの書き込みとDiscordの表示更新には数秒かかることがあります。

**失敗したときの見分け方**

- 選んだ直後にDiscordが「このアプリケーションは応答していません」「interaction failed」のように表示する: Workerが3秒以内に応答できていません。CloudflareのWorkerの画面で「Logs」（リアルタイムログ）を開き、エラーが出ていないか確認してください。7-1のPublic Keyの貼り間違いも同じ症状になります。
- 選択自体はできるが、表示が変わらない・分類が「更新に失敗しました」という文言に変わる: WorkerからGASまでは届いていますが、その先（Notionへの書き込み）が失敗しています。文言の後ろにエラーの内容が短く付くので、それを手掛かりにしてください（トークンやデータベースIDの設定ミスが多いです）。GASの「実行数」タブでも同じエラーを確認できます。
- 何も起きない（Discord側にも反応がない）: 7-6の「Interactions Endpoint URL」が保存できているか、7-2でBotがサーバーに入っているかを確認してください。

うまくいかない場合でも、手順6までの通知と記帳は影響を受けません。手順7を諦めても、Notionの表を直接編集すればいつでも分類を直せます。

## カード会社ごとの届き方

- **三井住友**: 利用の数分後に「ご利用のお知らせ」が届き、その時点で店名と金額が入ります。iD払いなど即時の通知が無い利用は、数日後の「ご利用明細のお知らせ」で1行増えます。明細の方に即時の通知と同じ日付・金額の利用が載っていたら、記帳済みとみなして増やしません。
- **楽天カード**: 利用の直後に「速報版」、数日後に「確定版」が届きます。速報版には店名が無いので読まず、確定版が届いた時に1行増えます。
- **ビューカード**: 利用の直後に「速報版」が届き、店名は「国内加盟店」のような総称です。約1週間後に「確報版」が届くと、同じ日付・金額で店名が総称のままの行を探し、店名を本当の名前に書き換えます。行は増えません。分類は「その他」のままの時だけ書き換え、手で選び直した分類は残します。速報版が届いていなかった利用は、確報版で1行増えます。同じ日付・金額・店名の行が既にある時は、書き換えも追加もしません。

カード会社からの請求額の案内や広告メールは、件名で見分けて読みません。

## できないこと（買う前に知っておくこと）

- 三井住友・楽天カード・ビューカード（JALカードSuica・ルミネカード・JRE CARD等）以外には対応していません。JAL・JCB／DC／AMEX発行のJALカード（Suicaが付いていないもの）は通知の送り主が異なり、対応していません。
- 外貨で書かれた金額は読みません。その通知には「要確認」の印が付きます。海外での利用でも、通知の金額が円で書かれていれば記帳されます。
- 取消・返品は家計簿から自動では減りません。三井住友の返品の通知には「要確認」の印が付きます。元の行を手で消してください。
- 三井住友の「ご利用明細のお知らせ」は1通に複数の利用が入ることがあります。読むのは最初の1件だけです。
- 三井住友で同じ日に同じ金額の利用が2回あり、片方が即時の通知、もう片方が明細だけで届いた場合は、明細の方が記帳されません。
- ビューカードで同じ日に同じ金額の利用が2回あり、片方の速報版が届かなかった場合は、その1回が記帳されないことがあります。
- ビューカードで同じ日に同じ店で同じ金額の利用が2回あった場合、2行目は店名が「国内加盟店」、分類が「その他」のまま残ります。手で書き換えてください。
- ビューカードで確報版が速報版より先に届いた場合は、同じ利用が2行になります。
- 家計簿に書き込めたのに、その後の処理が一時的な不調で失敗すると、次の回に同じ利用がもう1行記帳されることがあります。Notionからの応答だけが失敗した場合と、処理済みの記録の保存が続けて失敗した場合です。対象は三井住友の「ご利用のお知らせ」、楽天の確定版、ビューカードの速報版です。三井住友の明細とビューカードの確報版は、次の回に既存の行を見つけるので増えません。記録の保存は、最初の1回が失敗しても2回までやり直します。
- 通知メール自体が来ないカード（通知設定がオフの場合）は記帳されません。カード会社の設定でメール通知をオンにしてください。
- 分類は店名の言葉だけで決めます。「タッチ決済交通利用」のように店名がない通知は「その他」になります。Notion上で選び直してください。
- 楽天カードの確定版の記帳と、ビューカードの速報版・確報版を合わせる処理は、実際にGmailとNotionをつないで動かす確認がまだ済んでいません。
- 手順7を設定する前に届いた通知には、分類を選び直すセレクトメニューは付きません（設定した後の通知からです）。
- 手順7は環境によってDiscordへの通知投稿自体がDiscord側で拒否され、セレクトメニュー付きの通知が出ないことがあります（手順7内の「既知の不具合」参照）。記帳自体には影響しません。

## 困ったときは

「トークン」「データベースID」の値を貼り間違えていないか、まずスペースや改行が混ざっていないかを確認してください。それでも動かないときは、GASの「実行数」タブに出るエラーメッセージを手掛かりにしてください。
