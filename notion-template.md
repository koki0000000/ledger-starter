# Notionテンプレート定義書

Ledger Starterが書き込む先は、あなたのNotionワークスペースに自分で作るデータベース1個だけです。
この文書のとおりにプロパティを作れば準備は終わります。コピー元のテンプレートは配りません。他人が作ったページをあなたのワークスペースに持ち込まず、自分の手で作ってもらうためです。

## 作るもの

新規ページで「データベース — フルページ」を作成し、名前を「家計簿」など好きな名前にします。
デフォルトで「名前」（タイトル）は既にあるので、残り4つのプロパティを追加します。
Notionを英語表示で使っている場合、タイトル列の初期名は「Name」です。「名前」に改名してください。

| プロパティ名 | 型 | 設定 |
|---|---|---|
| 名前 | タイトル（既定） | 店名が入る |
| 金額 | 数値 | 表示形式は任意。書き込みには影響しない |
| 日付 | 日付 | そのまま |
| カード | セレクト | 選択肢は下記の3つを追加 |
| 分類 | セレクト | 選択肢は下記の10個を追加 |

プロパティ名は上表と**完全に一致**させてください。1文字でも違うと、README の手順3で貼るプログラムからの書き込みが失敗します。

## 「カード」の選択肢

プログラムが書き込む値と完全に一致させるため、選択肢名は次の3つだけにしてください。

- 三井住友
- 楽天カード
- ビューカード

「ビューカード」はJR東日本のカード発行会社です。JALカードSuica・ルミネカード・JRE CARD等はここに含まれます。JCB／DC／AMEX発行のJALカード（Suicaが付いていないもの）は含まれません。

## 「分類」の選択肢

店名からの自動推定に使う10個です。順不同で構いません。

- 食費
- 食料品
- 日用品
- ネット通販
- サブスク・娯楽
- 医療
- 光熱費
- 通信
- 交通
- その他

推定が外れていたら、Notion上でセレクトを選び直すだけで直せます（GAS側の修正は不要）。

## データベースIDの調べ方

作ったデータベースをブラウザで開き、URLを見ます。

```
https://www.notion.so/【ここの32文字】?v=...
```

`?v=` の直前にある、ハイフンなしの32文字の英数字がデータベースIDです。URLの先頭が `app.notion.com/p/` になっていたり、32文字の前にページ名が付いていたりすることがありますが、見る場所は同じで `?v=` の直前です。`Config.gs` の `NOTION_DATABASE_ID` に貼ります。
